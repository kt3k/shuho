{
  "fmt": {
    "files": {
      "include": [
        "bulbofile.js"
      ]
    }
  },
  "tasks": {
    "clean": "rm -rf build",
    "start": "deno run --compat --unstable -A node_modules/.bin/bulbo serve",
    "build": "deno run --compat --unstable -A node_modules/.bin/bulbo build $(git rev-parse --short HEAD)"
  }
}
